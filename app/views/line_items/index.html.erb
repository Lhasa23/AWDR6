<p id="notice"><%= notice %></p>

<h1>Cart</h1>

<table>
	<thead>
		<tr>
			<th>Title</th>
			<th>Price</th>
		</tr>
	</thead>

	<tbody class="line-items">
		<% @line_items.each do |line_item| %>
			<tr class="line-item">
				<td class="title"><%= line_item.product.title %></td>
				<td class="price">
					<%= number_to_currency(line_item.product.price, locale: 'zh-CN') %>
				</td>
			</tr>
		<% end %>
	</tbody>
</table>

<div class="total">
	<span>
		Total:
	</span>
	<span class="price">
		<% total_price = @line_items.sum { |line_item| line_item.product.price }.to_f %>
		<%= number_to_currency(total_price, locale: 'zh-CN') %>
	</span>
</div>
